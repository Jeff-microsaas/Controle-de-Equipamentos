rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissão completa para todas as coleções do sistema de controle de equipamentos
    // (users, months, history, system, e quaisquer subcoleções)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
